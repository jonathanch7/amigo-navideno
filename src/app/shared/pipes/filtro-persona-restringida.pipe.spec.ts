import { FiltroPersonaRestringidaPipe } from './filtro-persona-restringida.pipe';

describe('FiltroPersonaRestringidaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPersonaRestringidaPipe();
    expect(pipe).toBeTruthy();
  });
});
